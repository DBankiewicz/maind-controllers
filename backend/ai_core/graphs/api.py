def get_email_info(email_id: str) -> dict:
    # Get the email info

    # ask the rag for ids of emails related to this email_id (pass the content as context)

    # build the DAG

    # get rolling states

    # get topic tags

    # return all infor as a single large text description inside dict["text"]

    return {"text": "This is a placeholder response for email_id: " + email_id}